{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat application with Ollama integration",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "electron": "^24.0.0",
    "electron-store": "^8.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^20.0.0"
  }
}
